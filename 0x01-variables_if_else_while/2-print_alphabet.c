/**
 * *main - Print the last digit and do an if evaluation
 * *
 * *Return: Always Zero
 *
 *
 */
#include <stdio.h>
int main(void)
{
char c = 'a';
while (c <= 'z')
{
putchar(c);
c = c + 1;
}
putchar(10);
return (0);
}
